<UserControl x:Class="Wlst.Ux.Menu.Views.MenuView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" mc:Ignorable="d">
    
    <Grid>
        <!--<Grid.Resources >
            <HierarchicalDataTemplate x:Key="dt" ItemsSource="{Binding Path=Items}" >

                <StackPanel Orientation="Horizontal" Visibility="{Binding Visibility}">

                    <TextBlock Text="{Binding Path=Text}" Margin="5,0,5,0"></TextBlock>
             
                </StackPanel>
                
            </HierarchicalDataTemplate>

        </Grid.Resources>-->
        <Menu  MinWidth="800" MinHeight="30" VerticalAlignment="Top"  ItemsSource="{Binding Items}" >
        </Menu>
        <!--<StackPanel  Background="{x:Null }"  Height="30" VerticalAlignment="Top">
            <Menu  Background="{x:Null }"  ItemsSource="{Binding Items}" >
            </Menu>
        </StackPanel>-->
    </Grid>
</UserControl>